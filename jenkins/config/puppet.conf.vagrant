[main]

logdir = /var/log/puppet
rundir = /var/run/puppet
ssldir = $vardir/ssl
ca_server = puppet.scheekur.devops

[agent]

classfile = $vardir/classes.txt
localconfig = $vardir/localconfig

server = puppet.scheekur.devops
caserver = puppet.scheekur.devops
